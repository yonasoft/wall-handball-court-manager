<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yonasoft.handballcourtmanager">

    <application
        android:name="com.yonasoft.handballcourtmanager.HandballApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_adaptive_fore"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HandballCourtManager">
        <activity
            android:name="com.yonasoft.handballcourtmanager.MainActivity"
            android:exported="true"
            >
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>